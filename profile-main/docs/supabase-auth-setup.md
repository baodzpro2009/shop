# Supabase Auth Setup

1. Vào `Authentication > Users` trong Supabase.
2. Tạo một user admin bằng email thật của bạn.
3. Mở `assets/js/shared/app-config.js`.
4. Thay `admin@example.com` bằng email admin thật.
5. Dùng `docs/supabase-secure-setup.sql` để áp dụng policy an toàn cho bảng và bucket.
